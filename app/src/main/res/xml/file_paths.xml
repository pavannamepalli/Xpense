<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Internal files directory for stored receipt images -->
    <files-path name="receipt_images" path="." />
    
    <!-- Cache directory for temporary files -->
    <cache-path name="shared_files" path="." />
    
    <!-- External cache directory for larger files -->
    <external-cache-path name="external_shared_files" path="." />
    
</paths>
